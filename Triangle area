#include <bits/stdc++.h>
using namespace std;
class Triangle{
  float base,height;
public:
  Triangle(float b,float h) : base(b),height(h);
  float area(){
    return (0.5*base*height);
  }
};
int main(){
  float b,h;
  cin>>b>>h;
  Triangle a(b,h);
  cout<<a.area();
  return 0;
}
