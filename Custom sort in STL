#include <bits/stdc++.h>
using namespace std;

//custom sort according to absolute value
struct AbsValComp{
    bool operator()(int a, int b){
        return abs(a)<abs(b);
    }
};
int main(){
    set<int, AbsValComp> a;
    int n,b;
    cin>>n;
    for(int i=0;i<n;i++){
        cin>>b;
        a.insert(b);
    }

    for(int elem:a){
        cout<<elem<<endl;
    }
}
